---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Projets" description="Projets et créations d'un renard digital">
  <div class="projects-container">
    <h1 class="page-title">Projets<span class="accent">.</span></h1>
    <p class="subtitle">Créations et expérimentations digitales</p>

    <div class="projects-grid">
      <article class="project-card featured">
        <div class="project-icon">🌤️</div>
        <h2 class="project-title">Météo Renard</h2>
        <p class="project-description">
          Une application météo élégante avec conseils rusés de Renard.
        </p>
        <div class="project-links">
          <a href="/meteo" class="project-link primary">
            Voir l'application →
          </a>
        </div>
      </article>

      <article class="project-card">
        <div class="project-icon">🎲</div>
        <h2 class="project-title">Premier Joueur</h2>
        <p class="project-description">
          Déterminez qui commence la partie avec style. Lancez les dés et que le hasard décide !
        </p>
        <div class="project-links">
          <a href="/premier-joueur" class="project-link primary">
            Lancer les dés →
          </a>
        </div>
      </article>
    </div>
  </div>

  <style>
    .projects-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 2rem 2rem 4rem;
    }

    .subtitle {
      text-align: center;
      font-size: 1.2rem;
      color: rgba(255, 255, 255, 0.7);
      margin-bottom: 4rem;
      letter-spacing: 0.05em;
    }

    .projects-grid {
      display: flex;
      flex-direction: column;
      gap: 2rem;
      margin-bottom: 4rem;
      max-width: 800px;
      margin-left: auto;
      margin-right: auto;
    }

    .project-card {
      background: rgba(0, 0, 0, 0.3);
      border: 1px solid rgba(255, 107, 0, 0.1);
      border-radius: 16px;
      padding: 2rem;
      transition: all 0.3s ease;
      backdrop-filter: blur(10px);
      position: relative;
      overflow: hidden;
    }

    .project-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, #ff6b00, transparent);
      transform: translateX(-100%);
      transition: transform 0.5s ease;
    }

    .project-card:hover::before {
      transform: translateX(100%);
    }

    .project-card:hover {
      transform: translateY(-5px);
      border-color: rgba(255, 107, 0, 0.3);
      box-shadow: 0 10px 40px rgba(255, 107, 0, 0.15);
    }

    .project-card.featured {
      background: rgba(255, 107, 0, 0.05);
      border-color: rgba(255, 107, 0, 0.2);
    }

    .project-icon {
      font-size: 3rem;
      margin-bottom: 1.5rem;
      filter: drop-shadow(0 0 20px rgba(255, 107, 0, 0.5));
      animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }

    .project-title {
      font-family: "Cinzel", serif;
      font-size: 1.8rem;
      margin-bottom: 1rem;
      color: #fff;
    }

    .project-description {
      color: rgba(255, 255, 255, 0.8);
      line-height: 1.6;
      margin-bottom: 1.5rem;
      font-size: 1.05rem;
    }

    .project-links {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .project-link {
      color: rgba(255, 255, 255, 0.8);
      text-decoration: none;
      padding: 0.6rem 1.5rem;
      border: 1px solid rgba(255, 107, 0, 0.3);
      border-radius: 25px;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      background: rgba(0, 0, 0, 0.3);
    }

    .project-link:hover {
      background: rgba(255, 107, 0, 0.1);
      border-color: #ff6b00;
      color: #ff6b00;
      transform: translateX(3px);
    }

    .project-link.primary {
      background: rgba(255, 107, 0, 0.15);
      border-color: #ff6b00;
      color: #ff6b00;
    }

    .project-link.primary:hover {
      background: rgba(255, 107, 0, 0.25);
      box-shadow: 0 0 20px rgba(255, 107, 0, 0.3);
    }

    @media (max-width: 768px) {
      .projects-container {
        padding: 1rem 1rem 3rem;
      }

      .subtitle {
        font-size: 1rem;
        margin-bottom: 3rem;
      }

      .projects-grid {
        gap: 1.5rem;
      }

      .project-card {
        padding: 1.5rem;
      }

      .project-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
      }

      .project-title {
        font-size: 1.5rem;
      }

      .project-description {
        font-size: 1rem;
      }

      .project-link {
        font-size: 0.95rem;
        padding: 0.5rem 1.2rem;
      }
    }

    @media (max-width: 480px) {
      .project-card {
        padding: 1.25rem;
      }

      .project-links {
        flex-direction: column;
      }

      .project-link {
        text-align: center;
        justify-content: center;
      }
    }
  </style>
</BaseLayout>